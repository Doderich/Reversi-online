{
  "name": "server",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run index.ts",
    "build": "bun build ./index.ts --outdir ./build --target bun"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prisma": "^5.14.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@prisma/client": "^5.14.0",
    "socket.io": "^4.7.5"
  }
}